# MiCake.Templates
Template collection for MiCake-based applications.

## Available templates
- **StandardWeb** – opinionated ASP.NET Core + MiCake starter (DDD layers, JWT, Serilog, Scalar). See `src/StandardWeb/README.en.md` or `src/StandardWeb/README.zh-CN.md` for detailed guidance and sample commands.

## Getting started
```
dotnet new install .
dotnet new micake-standardweb -n Demo.StandardWeb
```
